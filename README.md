# Eddiecameron Brew

## How do I install these formulae?
`brew install eddiecameron/brew/<formula>`

Or `brew tap eddiecameron/brew` and then `brew install <formula>`.

## Documentation
`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
